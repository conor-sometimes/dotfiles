# if we're not on nara (desktop), ignore the following files/dirs
{{ if not (eq .chezmoi.hostname .hosts.desktop.hostname) }}
.xinitrc
.mozilla
.config/alacritty
.config/bspwm
.config/dunst
.config/gtk-2.0
.config/gtk-3.0
.config/mimeapps.list
.config/mpv
.config/ncmpcpp
.config/polybar
.config/redshift
.config/restic
.config/rofi
.config/sxhkd
.config/zathura
{{ end }}

# We only use systemd on linux (server/desktop), so ignore the directory otherwise
{{ if not (eq .chezmoi.os "linux") }}
.config/systemd
{{ end }}

# if we are not on mac or linux (generic) then we ignore the following dirs
{{ if not (or (eq .chezmoi.os "darwin") (eq .chezmoi.os "linux")) }}
.bin
.config/git
.config/ranger
.config/espanso
{{ end }}

# If we're not on $HOSTNAME, then we don't create the following dirs
#
# Desktop (Linux)
{{ if not (eq .chezmoi.hostname .hosts.desktop.hostname) }}
.ssh/keys/private/nara
{{ end }}

# Server
{{ if not (eq .chezmoi.hostname .hosts.server.hostname) }}
.ssh/keys/private/server
{{ end }}

# Macbook
{{ if not (eq .chezmoi.hostname .hosts.macbook.hostname) }}
.ssh/keys/private/macbook
.ssh/keys/private/macbook-work
{{ end }}

README.md
keys.txt
provision/

key.txt.age
